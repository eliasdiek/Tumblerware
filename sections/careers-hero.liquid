<style>
    .index-page_hero-section {
        background-color: {{ section.settings.bg_color }};
    }
    .page-hero-wrapper {
        min-height: calc(100vh - 102px);
        padding: 50px;
    }
    @media screen and (max-width: 768px) {
        .page-hero-wrapper {
            min-height: calc(100vh - 202px);
            padding: 60px;
        }
    }
</style>

<div class="page-hero-wrapper">
    <div class="section-header text-center">
        <p class="text-above-heading">{{ section.settings.above_heading_text }}</p>
        <h1>{{ section.settings.heading }}</h1>
    </div>
    <div class="section-body text-center">
        {{ section.settings.description }}

        <div class="section-cta">
            <a href="{{ section.settings.link }}" target="_blank">{{ section.settings.link_text }}</a>
        </div>
    </div>

    <div class="move-down">
        <a href="#" id="move-down-btn">
            <span></span>
        </a>
    </div>
</div>
  
{% schema %}
{
"name": "Page hero",
"class": "index-page_hero-section",
"settings": [
    {
        "type": "text",
        "id": "above_heading_text",
        "label": "Text above the heading",
        "default": "COME WORK WITH US"
    },
    {
        "type": "text",
        "id": "heading",
        "label": "Heading",
        "default": "A short sentence describing what someone will receive by heading"
    },
    {
        "type": "richtext",
        "id": "description",
        "label": "Description",
        "default": "<p>A short sentence describing what someone will receive by description</p>"
    },
    {
        "type": "text",
        "id": "link_text",
        "label": "Link text",
        "default": "See open positions"
    },
    {
        "type": "url",
        "id": "link",
        "label": "Link"
    },
    {
        "type": "color",
        "id": "bg_color",
        "label": "Background Color"
    },
    {
        "type": "select",
        "id": "padding_size",
        "default": "large",
        "label": "Padding size",
        "options": [
            {
                "value": "large",
                "label": "Large"
            },
            {
                "value": "small",
                "label": "Small"
            }
        ]
    }
],
"presets": [
    {
        "name": "Newsletter signup",
        "category": "Text"
    }
]
}
{% endschema %}
  