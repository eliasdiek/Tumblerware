<style>
    .index-text_with_button_2-section {
        background-color: {{ section.settings.bg_color }};
        padding: 50px 0;
        text-align: center;
    }
    .index-text_with_button_2-section .wrapper {
        max-width: 1280px;
    }
    {% if section.settings.heading_color != blank %}
    .index-text_with_button_2-section h2 {
        color: {{ section.settings.heading_color }};
    }
    {% endif %}
    {% unless section.settings.txt_color == blank %}
    .index-text_with_button_2-section p {
        color: {{ section.settings.txt_color }};
    }
    {% endunless %}
    .index-text_with_button_2-section p {
        font-size: 14px;
        line-height: 24px;
        font-weight: 400;
        margin-top: 10px;
    }
    .index-text_with_button_2-section .section-header {
        margin-top: 0;
    }
    .index-text_with_button_2-section .section-footer {
        margin: 10px 0 0 0;
    }
    {% unless section.settings.button_text_color == blank %}
    .index-text_with_button_2-section .btn_secondary {
        border-color: {{ section.settings.button_text_color }};
        color: {{ section.settings.button_text_color }};
    }
    .index-text_with_button_2-section a {
        color: {{ section.settings.button_text_color }};
    }
    {% endunless %}
    {% unless section.settings.button_color == blank %}
    .index-text_with_button_2-section .btn_secondary {
        background: {{ sections.settings.button_color }};
    }
    {% endunless %}
</style>

<div class="text_with_button_2-wrapper">
    <div class="wrapper">
        <div class="page-width">
            {% unless section.settings.heading == blank %}
            <div class="section-header">
                <h2 class="product_text1_new home-section-heading text-center">{{ section.settings.heading }}</h2>
            </div>
            {% endunless %}
        
            {% unless section.settings.body == blank %}
            <div class="section-body">
                {{ section.settings.body }}
            </div>
            {% endunless %}
        
            {% unless section.settings.button_link == blank and section.settings.button_text == blank %}
            <div class="section-footer">
                <a href="{{ section.settings.button_link }}" class="btn btn_secondary">{{ section.settings.button_text }}</a>
            </div>
            {% endunless %}
        </div>
    </div>
</div>

{% schema %}
{
"name": "Text with button (2)",
"class": "index-text_with_button_2-section",
"settings": [
    {
        "type": "text",
        "id": "heading",
        "label": "Heading",
        "default": "A short sentence describing what someone will receive by heading"
    },
    {
        "type": "richtext",
        "id": "body",
        "label": "Description",
        "default": "<p>A short sentence describing what someone will receive by body text</p>"
    },
    {
        "type": "url",
        "id": "button_link",
        "label": "Button link"
    },
    {
        "type": "text",
        "id": "button_text",
        "label": "Button text"
    },
    {
        "type": "color",
        "id": "bg_color",
        "label": "Background Color"
    },
    {
        "type": "color",
        "id": "heading_color",
        "label": "Heading color"
    },
    {
        "type": "color",
        "id": "txt_color",
        "label": "Text Color"
    },
    {
        "type": "color",
        "id": "button_color",
        "label": "Button color"
    },
    {
        "type": "color",
        "id": "button_text_color",
        "label": "Button text color"
    }
],
"presets": [
    {
        "name": "Text with button (2)",
        "category": "Text"
    }
]
}
{% endschema %}
  