<!-- Begin Shopify-Afterpay JavaScript Snippet (v1.0.10) -->
<script type="lazyload2">
// Overrides:
// var afterpay_min = 0.04;            // As per your Afterpay contract.
// var afterpay_max = 2000.00;         // As per your Afterpay contract.
// var afterpay_cbt_enabled = false;   // As per your Afterpay contract; change to true to display Cross-Border Trade artwork (for AU/NZ).
// var afterpay_logo_theme = 'colour'; // Can be 'colour', 'black' or 'white'.
  var amount = {{ product.price | json }};
  var afterpay_product_selector = amount > 600 ? '.product-single__add-to-cart': ''
// var afterpay_cart_integration_enabled = true;
// var afterpay_cart_static_selector = '#cart-subtotal-selector';

// Non-editable fields:
var afterpay_shop_currency = {{ shop.currency | json }};
var afterpay_cart_currency = {{ cart.currency.iso_code | json }};
var afterpay_shop_money_format = {{ shop.money_format | json }};
var afterpay_shop_permanent_domain = {{ shop.permanent_domain | json }};
var afterpay_theme_name = {{ theme.name | json }};
var afterpay_product = {{ product | json }};
var afterpay_current_variant = {{ product.selected_or_first_available_variant | json }};
var afterpay_cart_total_price = {{ cart.total_price | json }};
var afterpay_js_snippet_version = '1.0.10';
</script>
<script type="lazyload2" data-src="https://static.afterpay.com/shopify-afterpay-javascript.js"></script>
<!-- End Shopify-Afterpay JavaScript Snippet (v1.0.10) -->
<style>
  .afterpay-btn img {width: 158px !important;}
  .ajaxcart__footer .afterpay-btn {  
    width: 100% !important;
    display: block;
    height: 44px;
    background: #B2FCE4;
    text-align: center;
    vertical-align: center;
    margin-top: 10px;
    padding-top: 6px;
  }
  .afterpay-paragraph {
    background: white;
    padding: 8px 4px;
  }
  .afterpay-text1, .afterpay-text2, .afterpay-instalments {
    display: none !important;
  }
</style>
<script type="lazyload2">
  window.addEventListener("load", () => {
    let apAmount = document.querySelector(".afterpay-instalments");
    let newTextContent = " Shop now. Pay in 4 Installments of " + apAmount.textContent;
    let apText3 = document.querySelector(".afterpay-text3");
    apText3.textContent = newTextContent;
  });
</script>
